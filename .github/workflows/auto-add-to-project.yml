name: 🚀 Auto PRPRPRP
on:
  pull_request:
    

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: echo tree
        run: ls 
      - name: echo secrets
        run: echo '${{toJSON(secrets)}}'

       

   
