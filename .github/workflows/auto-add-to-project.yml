name: 🚀 Auto PRPRPRP
on:
  pull_request:
    

jobs:
  request:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.2
      - name: echo tree
        run: ls -al
      - name: gitaa
        run: |
          pwd
          cat .github/workflows/auto-add-to-project.yml
          git cat-file -p head
      - name: echo secrets
        run: echo 'hahah'

       

   
