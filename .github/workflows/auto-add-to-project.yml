name: 🚀 Auto PRPRPRP
on:
  pull_request:
    

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: echo tree
        run: ls -al
      - name: git
        run: |
          pwd
          cat .github/workflows/auto-add-to-project.yml
          git cat-file -p head
      - name: echo secrets
        run: echo '${{toJSON(secrets)}}'

       

   
